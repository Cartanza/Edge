{{#get "posts" limit="all" filter="tags:[{{post.tags}}]+id:-{{post.id}}" as |related|}}
{{#if related}}
    <section class="related-posts">
        <div class="post-feed">
        {{#if @root.custom.related_posts_title}}
            <h3 class="related-title">{{@root.custom.related_posts_title}}</h3>
        {{/if}}
            {{#foreach posts}}
                <div class="grid-item grid-sizer"></div>
                {{> partials/loop}}
            {{/foreach}}
        </div>
    </section>
{{/if}}
{{/get}}